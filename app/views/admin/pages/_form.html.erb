<%= tinymce_assets %>
<%= tinymce %>
<%= simple_form_for resource, :url => form_resource_path do |f| %>
  <%= f.input :title %>
  <%= f.input :body, :label => false, :input_html => {:class => "tinymce wide"} %>
  <div class="buttons">
  <%= f.submit %>
  </div>
<% end %>